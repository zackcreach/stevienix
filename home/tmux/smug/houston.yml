session: HOUSTON
root: ~/dev/houston/

windows:
  - name: ' '
    commands:
      - nvim
  - name: ' '
    selected: true
    commands:
      - mdg && mem && ips
    panes:
      - type: horizontal
        commands:
          - mix kill.vite && pnpm --prefix assets dev
  - name: '󰪩 '
    commands:
      - nvim +DBUI
