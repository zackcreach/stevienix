session: HOUSTON
root: ~/dev/houston/

before_start:
  - task d:glogin
  - yes y | task kinit

windows:
  - name: ' '
    commands:
      - nvim
  - name: ' '
    selected: true
    commands:
      - mdg && mem && ips
    panes:
      - type: horizontal
        commands:
          - mix kill.vite && pnpm --prefix assets dev
  - name: '󰪩 '
    commands:
      - nvim +DBUI
